#include <stdio.h>
/*
int main() {
	int unsigned_num = -1;
	if (((unsigned_num >> 1) & 1) == 1) {
		printf("이 PC시스템은 2의 보수를 사용합니다.");
		// unsigned_num 의 -1은 가장 낮은 가중치의 비트가 1이면 2의 보수를 사용하는 시스템이고 0 이면 1의 보수를 사용하는 시스템이다.
	}
	else {
		printf("이 PC시스템은 1의 보수를 사용합니다.");
	}
	return 0;
}
*/